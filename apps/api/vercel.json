{
  "version": 2,
  "installCommand": "cd ../.. && NODE_ENV=development pnpm install --shamefully-hoist && pnpm --filter @tuyendung/database db:generate && pnpm --filter @tuyendung/api build",
  "builds": [
    {
      "src": "api/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.js"
    }
  ]
}
