{
  "installCommand": "cd ../.. && pnpm install && cd packages/database && pnpm exec prisma generate",
  "buildCommand": "cd ../.. && pnpm --filter @tuyendung/api build",
  "outputDirectory": ".",
  "functions": {
    "api/index.js": {
      "maxDuration": 10
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index.js" }
  ]
}
