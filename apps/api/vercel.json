{
  "installCommand": "cd ../.. && pnpm install && cd packages/database && npx prisma generate",
  "buildCommand": "cd ../.. && pnpm --filter @tuyendung/api build",
  "outputDirectory": ".",
  "functions": {
    "api/index.js": {
      "maxDuration": 10
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index.js" }
  ]
}
